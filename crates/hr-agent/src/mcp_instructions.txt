Dataverse MCP — ALWAYS use these tools for all database operations on /root/workspace/.dataverse/app.db. NEVER modify the database file directly. Use list_tables and describe_table to inspect before modifying. Schema tools: create_table, add_column, remove_column, create_relation, drop_table. Data tools: query_data, insert_data, update_data, delete_data, count_rows. For application runtime code, connect a SQLite library to the same database path.